__pycache__/

# Research/calibration SQLite datasets (research/ scripts default their
# --db here). The live loop's own DB lives under live/logs/ (ignored there).
*.db
*.db-journal
*.db-wal
samples.db
